import { SafeAreaView, StatusBar, StyleSheet, Text, View } from "react-native";
interface ChatProps {

}

const Chat = ({ }: ChatProps) => {
  return ( 
    <View>
      <Text>Chat</Text>
    </View>
  );
}

export default Chat;